# 專案重要記憶與背景上下文 (AI Context)

這份檔案紀錄了「公司 LINE 天氣通知及帆布控制」專案的核心架構與重要邏輯，旨在幫助 AI 在新的對話中能快速接手開發。

---

## 📌 專案基本設定

- **主程式**：[rain_monitor.py](file:///e:/2026andigravity2/12-製作/公司line通知/rain_monitor.py)
- **設定檔**：[config.json](file:///e:/2026andigravity2/12-製作/公司line通知/config.json)
- **狀態檔**：[state.json](file:///e:/2026andigravity2/12-製作/公司line通知/state.json)
- **GitHub Actions 排程**：[.github/workflows/monitor.yml](file:///e:/2026andigravity2/12-製作/公司line通知/.github/workflows/monitor.yml)
- **監控地點**：台南市安南區慶和路二段 58 號（經緯度：`23.0485, 120.186`）
- **CWA 中央氣象署雨量測站**：安南測站（ID: `C2O950`），距離廠區約 5.25 公里。
- **廠區在氣象署雷達回波圖上的位置**：像素坐標 `(X: 1623, Y: 1941)`，讀取 `O-A0058-001.png`（解析度 3600x3600）進行矩陣切片分析。

---

## ⚙️ 核心業務邏輯：保守防呆模式 (Conservative Defense Mode)

為了防止局部性降雨造成模具受潮損壞，系統採取極為保守的防呆邏輯。

### 🔴 1. 觸發「加蓋帆布」條件（滿足任一即可）
系統在**未加蓋**狀態下，只要滿足以下任一降雨訊號，即判定為下雨並發送 **「🔴 請加蓋帆布」** LINE 通知，且狀態轉為已加蓋：
1. **天氣現象 (Weather Description)**：氣象署測站回傳的天氣描述中，含有 `雨`、`小雨`、`細雨`、`陣雨`、`毛毛雨` 等關鍵字。
2. **測站雨量計**：過去 10 分鐘雨量值大於 `0.0 mm` 或為微量降雨 `T`。
3. **雷達回波 (10公里半徑防線)**：以廠區坐標為中心，**半徑 10 公里範圍內**（約雷達圖 28 像素半徑），色差大於等於 `20` 的降雨回波點數達 **3 個點（含）以上**。

### 🟢 2. 觸發「暫不加蓋（解除）」條件（必須同時滿足）
系統在**已加蓋**狀態下，必須同時符合以下所有安全指標，才會發送 **「🟢 暫不加蓋帆布」** LINE 解除通知：
1. **天氣現象無雨**：天氣描述**不含**上述降雨關鍵字。
2. **雨量計無雨**：過去 10 分鐘雨量值為 `0.0 mm`。
3. **雷達回波無雨 (20公里半徑安全區)**：以廠區為中心，擴大至**半徑 20 公里範圍內**（約雷達圖 56 像素半徑），回波點數小於 3 個點。*(預防未來 30 分鐘內即將有降雨移入)*
4. **停雨冷卻時間滿 60 分鐘**：距離最後一次判定有雨的時間，已**持續且累積達 60 分鐘以上**。*(防止天氣忽晴忽雨導致訊息頻繁發送打擾現場)*

---

## ⏰ 排程與運作時間

- **監測頻率**：**每 10 分鐘執行一次**。為了錯開整點排隊延遲，設定為每小時的第 `2, 12, 22, 32, 42, 52` 分鐘執行。
- **偵測時段**：週一至週五非國定假日的 **07:30 至 19:30**。
- **補班與假日判定**：
  - 排除週末及台灣國定假日。
  - 優先載入 `config.json` 中的 `calendar_2026`（東陽公司特定行事曆），包含公司規定的國定假日排除，以及補班上班日放行。
- **每日自動重置**：每天晚上 **23:00 之後** 的第一次執行，會強制將狀態檔中的 `is_covered` 重置為 `false`，並清空 `last_rain_time`，確保隔天開始時是初始狀態。

---

## 🔑 環境變數與認證
部署於 GitHub Actions 時需設定以下 Secrets：
- `LINE_CHANNEL_ACCESS_TOKEN`：LINE 官方帳號 API Access Token
- `LINE_GROUP_ID`：發送目標 LINE 群組 ID（支援多個群組，以英文逗號 `,` 分隔）
- `CWA_API_KEY`：中央氣象署 Open Data API Key
- `GMAIL_USER` / `GMAIL_APP_PASSWORD`：用於系統出錯時發送 Gmail 警報信給管理員的信箱與應用程式密碼
- `config.json` 中的 `email.recipient`：指定警報信件的收件地址
